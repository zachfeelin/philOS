//import java.awt.event.MouseAdapter;
//import java.awt.event.MouseEvent;

public class philOSmain {
    public static void main(String[] args) {
        // Create the background window
        /*BackgroundWindow background = */new BackgroundWindow();

        // Create the foreground window
        //ForegroundWindow foreground = new ForegroundWindow();

        // Add a mouse listener to the background window to keep the foreground window on top
        /*background.addMouseListener(new MouseAdapter() {
            public void mousePressed(MouseEvent e) {
                foreground.toFront();
            }
        });*/
    }
}